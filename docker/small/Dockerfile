# small — Alpine base only (~5 MB)
FROM alpine:3.19
LABEL org.opencontainers.image.title="supply-chain-small"
LABEL org.opencontainers.image.description="Phase 4b size test — small (~5MB)"
CMD ["sh", "-c", "echo small-image hostname=$(hostname)"]