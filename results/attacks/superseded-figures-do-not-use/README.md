# Superseded figures â€” DO NOT USE

Screenshots of the *old* attack-simulation summary, before the workflow was
rewritten to derive its results from recorded observations (defect D4). They
show a hardcoded six-attack summary that contradicts the current Table 6.3, and
`attacks-azure.png` depicts an Azure run whose data was invalidated and deleted
(defect D18) â€” it is not evidence that Azure was ever evaluated.

Kept only so the record of what was once published is not silently erased.
**They must not appear as figures in the dissertation.** Defect D11.

The current attack evidence is `results/attacks/attack-results-aws.json`, and the
table is rendered by `scripts/render-all-tables.sh`.
